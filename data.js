import { startcount, info, deaths } from './countup.js'
import { startLoad, loadingdone } from './loading.js'



export function countyResults(COUNTY_NAME, STATE_NAME)	{
	console.log(COUNTY_NAME, STATE_NAME)
}